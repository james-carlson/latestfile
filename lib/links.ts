// One place for the repository URLs, so the site and its call-to-actions stay
// consistent if the repo ever moves.

export const REPO = "https://github.com/james-carlson/latestfile";
export const ISSUES_NEW = `${REPO}/issues/new/choose`;
export const ISSUES = `${REPO}/issues`;

/** Prefilled spec-feedback issue. */
export const SPEC_ISSUE = `${REPO}/issues/new?template=spec-feedback.yml`;
/** Prefilled registry-entry issue. */
export const REGISTRY_ISSUE = `${REPO}/issues/new?template=registry-entry.yml`;
